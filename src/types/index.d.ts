import { SimpleIcon } from 'simple-icons'

type SimpleIconData = {
  title: SimpleIcon['title']
  hex: SimpleIcon['hex']
  source: SimpleIcon['source']
  slug?: SimpleIcon['slug']
  guidelines?: SimpleIcon['guidelines']
}
